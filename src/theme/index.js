import {darkTheme} from './darkTheme';
import {lightTheme} from './lightTheme';

export const THEME = {
	'light': lightTheme,
	'dark': darkTheme,
}

export const THEME_NAME = {
	LIGHT: 'light',
	DARK: 'dark'
}